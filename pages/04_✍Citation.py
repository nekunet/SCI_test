import streamlit as st


def run():
    st.set_page_config(
        page_title="Citation",
        page_icon="✍️",
    )

st.write("# Citation")

st.markdown(
        """
        Link to Pubmed
    """
    )
